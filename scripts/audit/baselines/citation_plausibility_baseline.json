{
  "note": "Per-guide baseline for citation-plausibility warnings from GPT-4o-mini. Empty until OPENAI_API_KEY is added to the repo secrets and CI runs the plausibility check. Update via check_citation_plausibility.py --update-baseline.",
  "counts": {}
}
